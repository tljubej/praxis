// AoC corpus fixture: blank-line-separated sections of CSV integers.
// Input:
//   "1,2,3\n4,5,6\n\n7,8,9\n"
// → parses as Vec[Vec[Vec[Int]]] (sections → lines → csv tokens).
var groups = read sections(lines(csv(int)));
out(groups.len())
