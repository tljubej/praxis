error: program faulted: size or extent out of range

Backtrace:
#0   <entry>

  locals:
    n: Int = -1
    v: Vec[Int] = <uninit>
  temps:
    <tmp#1: Int> @ "0" = <uninit>
    <tmp#2: Int> @ "1" = 1
    <tmp#3: Int> @ "0 - 1" = -1
    <tmp#5: Int> @ "0" = 0
    <tmp#6: Vec[Int]> @ "Vec(n, 0)" = <uninit>
    <tmp#8: Unit> @ "out(v)" = <uninit>
    <tmp#9: Unit> @ "// A size is an ordinary `Int` computed at run time, so a negative one is not // something `praxis check` can refuse. It is a fault. var n = 0 - 1 var v = Vec(n, 0) out(v)" = <uninit>