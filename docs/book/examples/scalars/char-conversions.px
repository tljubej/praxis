// Char and Int convert in both directions, and nothing else converts.
var digits = "2026"
var value = 0
for c in digits {
    value = value * 10 + (c.to_int() - "0"[0].to_int())
}
out(value)
out("A"[0].to_int())
out(65.to_char())
out(233.to_char())
