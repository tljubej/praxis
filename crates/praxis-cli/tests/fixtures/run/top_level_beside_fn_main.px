// A declared `main` is an ordinary function (ADR-154). It runs here because the
// top level calls it, once — the host does not single the name out, and would
// have nothing to run in this file if the call were removed.
out(1)

fn main() {
    out(2)
}

main()
out(3)
