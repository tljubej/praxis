# Clippy policy. Shared by `just clippy` and CI (`just ci`).
# `msrv` must equal `Cargo.toml`'s `rust-version`. When the two disagree this file
# wins, and every MSRV-aware lint above the lower number is silently skipped.
msrv = "1.88"
