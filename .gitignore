/target
# Insta pending snapshot updates (review and accept, never commit these).
*.snap.new
*.pending-snap
# Editor / OS / tooling clutter.
.vscode/
.idea/
.DS_Store
.zcode/

# Agent worktrees (git worktree checkouts live here; never part of a commit)
.claude/worktrees/
